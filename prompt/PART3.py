#!/usr/bin/env python3
# -*- coding: utf-8 -*-

"""
PART3 已简化：不再读取历史 SQL 与延迟结果。
保留为空，实现由 COMBINATION 直接拼接固定的尾部文本。
"""

from typing import Optional


def build_part3(sql_dir: Optional[str] = None, latency_path: Optional[str] = None) -> str:
    return ""


def main() -> None:
    # 兼容旧 CLI：直接打印空内容
    print("")


if __name__ == "__main__":
    main()
